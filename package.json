{
  "name": "@scola/api-log",
  "version": "1.0.2",
  "description": "API Log",
  "repository": "github:scola84/node-api-log",
  "author": "M.C. van den Boogaart <scola@kvdb.name>",
  "license": "MIT",
  "main": "./dist/api-log.js",
  "jsnext:main": "index.js",
  "devDependencies": {
    "rollup": "^0.36.1",
    "rollup-plugin-buble": "^0.14.0",
    "uglify-js": "^2.7.3"
  },
  "scripts": {
    "build": "npm run cleanup && npm run rollup",
    "build:min": "npm run build && npm run min",
    "cleanup": "rm -rf dist",
    "min": "uglifyjs dist/api-log.js -m -o dist/api-log.min.js",
    "prepublish": "npm run build:min",
    "rollup": "rollup -c"
  }
}
